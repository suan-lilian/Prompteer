{
  "name": "prompteer",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "prepare": "husky",
    "dev": "scripts/dev.sh",
    "dev:legacy": "scripts/dev-legacy.sh",
    "build": "turbo build",
    "build:legacy": "pnpm --filter @prompteer/web-legacy build",
    "lint": "turbo lint",
    "lint:legacy": "pnpm --filter @prompteer/web-legacy lint",
    "typecheck": "turbo typecheck",
    "typecheck:legacy": "pnpm --filter @prompteer/web-legacy typecheck",
    "test": "turbo test",
    "audit": "pnpm audit --audit-level moderate",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "hooks:pre-commit": "pnpm exec lint-staged && uv run --project apps/api pre-commit run --all-files",
    "source-headers": "node scripts/check-source-headers.mjs",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "prettier": "^3.5.3",
    "turbo": "^2.5.4"
  },
  "pnpm": {
    "overrides": {
      "postcss": "8.5.15"
    }
  }
}
